# Public behavior contracts

These cases define observable routing and delivery expectations. They are not
model-output snapshots: an evaluator should check whether a run respects the
reference contract, text precision boundary, design fingerprint and visual
quality rubric.

Use [behavior-cases.jsonl](behavior-cases.jsonl) for routing checks and
[visual-rubric.md](visual-rubric.md) for generated-image review.
